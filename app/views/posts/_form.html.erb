
    <%= form_for(@post) do |f| %>
      <% if @post.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>
    
          <ul>
          <% @post.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      <% end %>
      <div class="form-group">
        <label for="name">
          Title
        </label>
        <%= f.text_field :title, :class => "form-control", :placeholder => "Blog Title" %>
      </div>
      
      <div class="form-group">
        <label for="phone">
          Content
        </label>
         <%= f.text_area :body, :rows=>8, :class => "form-control", :placeholder => "Blog Content" %>
        
        </textarea>
      </div>
      
      <button class="btn btn-info" type="submit">
        Submit
      </button>
      &emsp;&emsp;
      <%if params[:action] == "edit"%>
        <%= link_to 'Show', @post, :class => "btn btn-primary"%>
      <%end%>
      <%= link_to 'Back', posts_path, :class => "btn btn-warning" %>
    <% end %>

  